language: node_js
node_js:
  - 0.12
branches:
  only:
    - develop
env:
  - NODE_ENV=test
before_script:
  - npm install -g bower
  - npm install -g mocha
  - npm install -g karma
  - npm install -g karma-cli
  - bower install
  - mysql -e "create database IF NOT EXISTS openboard_tests;" -uroot
services:
  - mysql
